/**
 * Controller-oriented export for camera photo submission orchestration.
 * Keeps screen components thin while preserving backward-compatible hook behavior.
 */
export {
  usePhotoSubmissionFlow,
} from './use-photo-submission-flow'
