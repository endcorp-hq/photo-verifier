FROM node:20-alpine

WORKDIR /app

# Install dependencies
RUN npm install -g pnpm

COPY package.json pnpm-lock.yaml* ./
RUN pnpm install --frozen-lockfile || pnpm install

# Copy source
COPY . .

EXPOSE 3000

CMD ["node", "scripts/run-presign.js"]
